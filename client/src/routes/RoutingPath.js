/**
 * Diffrent routing paths.
 */
const universeView = '/';
const planetsView = '/planets';
const aboutView = '/about';

const RoutingPath = {
  universeView,
  planetsView,
  aboutView,
};

export default RoutingPath;
